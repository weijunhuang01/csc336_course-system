PORT=5000
DB_HOST=localhost
DB_PORT=3306
DB_USER=root
DB_PASSWORD=Psw4mysql
DB_NAME=student_registration
JWT_SECRET=replace-with-a-long-random-secret

# Optional: require this header on GET /api/admin/revenue-stats (X-Admin-Dashboard-Token). Match VITE_ADMIN_DASHBOARD_TOKEN in frontend/.env for production.
# ADMIN_DASHBOARD_TOKEN=
